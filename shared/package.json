{
  "name": "alexa-todoist-sync",
  "version": "2.0.0",
  "description": "Syncs Alexa Shopping List items to Todoist (runs locally)",
  "main": "sync.js",
  "scripts": {
    "start": "node sync.js",
    "test": "node sync.js --dry-run"
  },
  "dependencies": {
    "puppeteer": "^24.15.0",
    "puppeteer-extra": "^3.3.6",
    "puppeteer-extra-plugin-stealth": "^2.11.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
